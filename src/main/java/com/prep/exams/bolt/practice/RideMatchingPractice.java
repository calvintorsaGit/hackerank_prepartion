package com.prep.exams.bolt.practice;

/**
 * Bolt Exam: Ride Matching
 *
 * Match N riders to N drivers on a 1D line to minimize total distance.
 * Rider i is at position riders[i], driver j at drivers[j].
 *
 * Return the minimum total distance across all matchings.
 *
 * Constraints:
 * - riders.length == drivers.length
 * - 1 <= n <= 10^5
 * - Positions can be negative
 *
 * Example:
 * riders = [1, 5, 10], drivers = [2, 6, 9]
 * Output: 3 (|1-2| + |5-6| + |10-9| = 1+1+1)
 */
public class RideMatchingPractice {

    public static long minimumTotalDistance(int[] riders, int[] drivers) {
        // YOUR SOLUTION HERE
        return 0;
    }
}
