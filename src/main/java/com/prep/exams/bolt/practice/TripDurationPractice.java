package com.prep.exams.bolt.practice;

/**
 * Bolt Exam: Trip Duration
 *
 * Given a city road network as a weighted bidirectional graph,
 * find the shortest travel time from source to target.
 *
 * Input:
 * - n: number of nodes (0-indexed)
 * - edges: [[from, to, weight], ...]
 * - source, target: node indices
 *
 * Return shortest time, or -1 if unreachable.
 *
 * Constraints:
 * - 1 <= n <= 10^4
 * - All weights > 0
 *
 * Example:
 * n=4, edges=[[0,1,2],[1,3,3],[0,2,4],[2,3,1]], source=0, target=3
 * Output: 5 (path: 0->1->3, cost 2+3)
 */
public class TripDurationPractice {

    public static int shortestTime(int n, int[][] edges, int source, int target) {
        // YOUR SOLUTION HERE
        return -1;
    }
}
