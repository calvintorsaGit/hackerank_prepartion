package com.prep.exams.bolt.practice;

/**
 * Bolt Exam: Scooter Rebalancing
 *
 * Scooters are distributed across zones. Each zone must have
 * at least minPerZone scooters. A "move" = relocating 1 scooter
 * from a surplus zone to a deficit zone.
 *
 * Return the minimum number of moves needed.
 *
 * Constraints:
 * - 1 <= zones <= 10^5
 * - 0 <= zoneCounts[i] <= 10^6
 *
 * Example:
 * zoneCounts = [8, 2, 1, 5], minPerZone = 4
 * Output: 5 (zone1 needs 2, zone2 needs 3)
 */
public class ScooterRebalancingPractice {

    public static int minimumMoves(int[] zoneCounts, int minPerZone) {
        // YOUR SOLUTION HERE
        return 0;
    }
}
